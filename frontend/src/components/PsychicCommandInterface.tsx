
import React from 'react';

interface PsychicCommandInterfaceProps {
  onIntentDetected: (intent: string) => void;
}

export function PsychicCommandInterface({ onIntentDetected }: PsychicCommandInterfaceProps) {
  return (
    // Placeholder for psychic command interface
    <></>
  );
}
